import { defineConfig } from "astro/config";

// Update `site` if your GitHub username or deployment target differs.
export default defineConfig({
  site: "https://doview.github.io",
  base: "/jump-rope-guide",
});
